/* const INITIAL_STATE = {
  score: 0,
};

export default function questions(state = INITIAL_STATE, action) {
  switch (action.type) {
  case 'ADD_SCORE':
    return {
      ...state, score: action.score,
    };
  default:
    return state;
  }
}
 */
